apiVersion: 1

providers:
  # Fichier de configuration qui contient la liste des tableaux de bord à charger
  - name: 'Default'
    orgId: 1
    folder: 'FHIRHub'
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: true