apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
    
  - name: FHIRHub Logs
    type: simplejson
    access: proxy
    url: http://fhirhub:9091/api/logs
    isDefault: false
    editable: true
    jsonData:
      timeField: "timestamp"
      
  - name: Loki
    type: loki
    access: proxy
    url: http://fhirhub:9091/loki
    isDefault: false
    editable: true
    jsonData:
      maxLines: 1000